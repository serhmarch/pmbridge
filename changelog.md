# 0.2.0

* Fixed config parser bug when read serial server settings.
* Add `ipaddr` (TCP), `units` and `broadcast` params for `SERVER` command
* Add cmdline options `--print-config` and `--print-config-only`
* Add SIGTERM handler to gracefully stop the server
* Update `README.md` with new options and features
* Add tests for new features and bug fixes